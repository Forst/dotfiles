- defaults:
    link:
      create: true
      relink: true
      force: true
    shell:
      quiet: true
      stdout: true
      stderr: true
    create:
      mode: 0750

- link:
    ~/.gnupg/gpg-agent.conf: configs/gnupg/gpg-agent.conf
